// Initialize variables
moving = false; // Controller variable to set when the ball is moving.
magnet = false; // Controller variable to set when the magnet powerup is active.
spd = BALL_START_SPEED; // Base speed
dir = random_range(45, 135); // Base direction
