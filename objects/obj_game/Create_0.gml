// Initialize global variables
global.player_score = 0;
global.high_score = 0;
global.player_lives = 3;

// Initialize variables
state = "START";

if (room = rm_level_001) {
	audio_play_sound(snd_mus_omake2, 0, true, 0.5, 0, 1, 1);
}
